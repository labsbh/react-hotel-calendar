import { ReactElement } from 'react';
import { HotelCalendarProps } from '../typings';
declare const HotelCalendar: (props: Partial<HotelCalendarProps>) => ReactElement;
export default HotelCalendar;
